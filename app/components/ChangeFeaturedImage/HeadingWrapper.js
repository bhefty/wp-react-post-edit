import styled from 'styled-components'

export default styled.div`
  display: flex;
  justify-content: space-between;

  .btn-cancel {
    height: 35px;
    border: none;
    background: none;
    cursor: pointer;
  }
`
